<resources>
    <style name="AppTheme" parent="@android:style/Theme.NoTitleBar.Fullscreen"/>

    <!-- ignore actionbar in SDK < 14-->
    <style name="ActionBar" parent="@android:style/Theme.NoTitleBar"/>
</resources>
